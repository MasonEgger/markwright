{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"markwright","text":"<p>markwright is a set of Python-Markdown extensions plus a companion CLI, <code>mw</code>, for author-friendly markdown: highlighted text, labeled code fences, and media embeds. Use the extensions in any tool built on Python-Markdown (including MkDocs), or the <code>mw</code> pipeline in toolchains that are not, like Hugo.</p> <p>The author-facing syntax and HTML output are derived from DigitalOcean's <code>do-markdownit</code> (licensed under the Apache License 2.0); markwright ports those to Python-Markdown and adds the <code>mw</code> CLI on top. See NOTICE for attribution.</p>"},{"location":"#features","title":"Features","text":"<ul> <li>Highlight wraps <code>text</code> in <code>&lt;mark&gt;</code>, and works inside inline code and fenced code blocks where <code>==text==</code> cannot reach.</li> <li>Fence adds labels, secondary labels, environment classes, and command-line prefixes to code blocks.</li> <li>YouTube turns <code>[youtube ID]</code> into a responsive iframe embed.</li> <li>CodePen embeds a pen with theme, default-tab, and layout options.</li> <li>Twitter embeds a tweet with theme and alignment options.</li> <li>Instagram embeds a post with caption and alignment options.</li> <li>Slideshow builds an image slideshow with navigation arrows.</li> <li>Image Compare renders two images with a draggable before/after slider.</li> </ul>"},{"location":"#installation","title":"Installation","text":"<pre><code>uv add markwright\n</code></pre> <p>Or install from a local path:</p> <pre><code>uv add --editable ../markwright\n</code></pre> <p>MkDocs 2.0 Compatibility</p> <p>MkDocs 2.0 removes the plugin system and rewrites the theming architecture, breaking all existing plugins and themes including Material for MkDocs. There is no migration path. This project pins MkDocs 1.x (<code>mkdocs&gt;=1.6,&lt;2</code>).</p>"},{"location":"#usage","title":"Usage","text":"<p>These extensions work with Python-Markdown directly, so you can use them in any Python program (a Flask app, a CLI tool, a static site generator, or a simple script).</p> <pre><code>import markdown\n\nmd = markdown.Markdown(extensions=[\n    \"pymdownx.superfences\",\n    \"pymdownx.highlight\",\n    \"markwright.highlight\",\n    \"markwright.fence\",\n    \"markwright.youtube\",\n    \"markwright.codepen\",\n    \"markwright.twitter\",\n    \"markwright.instagram\",\n    \"markwright.slideshow\",\n    \"markwright.image_compare\",\n])\n\nsource = \"\"\"\n```command\n[label deploy.sh]\nssh root@server\napt update\n```\n\"\"\"\n\nhtml = md.convert(source)\nprint(html)\n</code></pre> <p>You can also load only the extensions you need by listing just those in the <code>extensions</code> list.</p> <p>Using MkDocs? See Using with MkDocs for the <code>mkdocs.yml</code> configuration.</p> <p>Note</p> <p>The fence extension should be used alongside <code>pymdownx.superfences</code> and <code>pymdownx.highlight</code> for proper code block rendering. Without them, the fence preprocessor will still extract directives, but the code block HTML structure may differ from what the postprocessor expects.</p>"},{"location":"#using-it-outside-python","title":"Using It Outside Python","text":"<p>If your toolchain is not built on Python-Markdown, the <code>mw</code> command-line tool runs the same extensions as pre and post filters around any renderer:</p> <pre><code>mw pre &lt; in.md | your-renderer | mw post &gt; out.html\n</code></pre> <p>See the CLI Reference for the commands and flags, and the Hugo integration guide for a worked setup.</p>"},{"location":"#architecture","title":"Architecture","text":"<p>Each extension is a standalone Python-Markdown extension loaded by name (e.g., <code>markwright.highlight</code>). The extensions use three processor patterns:</p> <ol> <li> <p>Fence uses a Preprocessor (priority 40) that runs before <code>pymdownx.superfences</code> to extract directives. A Postprocessor (priority 25) injects labels, classes, and prefix markup into rendered HTML.</p> </li> <li> <p>Embeds use Preprocessors (priority 20) that run after superfences, matching standalone <code>[name ...]</code> lines. Social embeds add a Postprocessor (priority 15) for one-time script injection.</p> </li> <li> <p>Highlight uses an InlineProcessor (priority 175) for regular text, plus a Postprocessor (priority 25) for <code>&lt;^&gt;</code> markers inside code blocks where the delimiters are HTML-escaped.</p> </li> </ol>"},{"location":"#license","title":"License","text":"<p>This project is a port of do-markdownit by DigitalOcean, which is licensed under the Apache License 2.0. See the NOTICE file for attribution details.</p>"},{"location":"cli/","title":"CLI Reference (<code>mw</code>)","text":"<p>The <code>mw</code> command exposes the markwright extensions as Unix filters, so their Markdown syntax works in toolchains that are not built on Python-Markdown. It is a stdin-to-stdout tool: every transform subcommand reads standard input and writes standard output, so it composes in any pipe. Input and output are UTF-8.</p> <p>The console script ships with the package. After <code>uv add markwright</code> (or <code>pip install markwright</code>), the <code>mw</code> command is on your path.</p>"},{"location":"cli/#subcommands","title":"Subcommands","text":"<pre><code>mw pre    [--use NAME ...] [--exclude NAME ...]\nmw post   [--use NAME ...] [--exclude NAME ...] [--warn]\nmw render [--use NAME ...] [--exclude NAME ...]\nmw list\nmw --version\n</code></pre>"},{"location":"cli/#mw-pre","title":"<code>mw pre</code>","text":"<p>Reads Markdown source and writes Markdown with the source-stage transforms applied. It expands the embed directives (<code>[youtube ...]</code>, <code>[codepen ...]</code>, and the rest) into raw HTML and extracts fence directives into an <code>&lt;!-- mw-fence:{JSON} --&gt;</code> comment that the post stage reads back later. With the highlight extension active, it also wraps prose <code>...</code> runs in <code>&lt;mark&gt;</code>, leaving in-code markers for the post stage.</p> <p>The output is meant to feed your renderer. Because the expanded embeds and the <code>&lt;mark&gt;</code> wrappers are raw HTML, your renderer must pass raw HTML through. See Renderer Requirements.</p>"},{"location":"cli/#mw-post","title":"<code>mw post</code>","text":"<p>Reads rendered HTML and writes HTML with the HTML-stage transforms applied. It styles fence code blocks from their <code>mw-fence</code> markers (label divs, environment classes, <code>&lt;ol&gt;&lt;li data-prefix&gt;</code> wrapping), wraps any remaining highlight markers in <code>&lt;mark&gt;</code>, and injects each embed script exactly once.</p> <p>The post stage is the complete path on its own. It needs no marker from the pre stage to inject scripts: it detects each embed's class signature in the rendered HTML, so it also works on hand-authored embeds. Running post twice does not double-inject a script or double-wrap a mark.</p>"},{"location":"cli/#mw-render","title":"<code>mw render</code>","text":"<p>Runs the full Markdown-to-HTML pipeline in one shot using the in-process Python-Markdown path. This is the standalone renderer for callers who do not have their own. It builds a <code>markdown.Markdown</code> with <code>pymdownx.superfences</code> and <code>pymdownx.highlight</code> plus the selected <code>markwright.*</code> extensions, matching the bundled site stack.</p>"},{"location":"cli/#mw-list","title":"<code>mw list</code>","text":"<p>Prints every registered extension and the stages it provides.</p> <pre><code>$ mw list\nyoutube: pre\nslideshow: pre\nimage_compare: pre\ncodepen: pre, post\ntwitter: pre, post\ninstagram: pre, post\nfence: pre, post\nhighlight: pre, post\n</code></pre> <p>An extension labeled <code>pre</code> has only a source-stage transform. An extension labeled <code>pre, post</code> participates in both stages.</p>"},{"location":"cli/#flags","title":"Flags","text":""},{"location":"cli/#-use-name","title":"<code>--use NAME</code>","text":"<p>Restrict the run to the named extension. The flag is repeatable, so <code>--use youtube --use highlight</code> runs exactly those two. The default, with no <code>--use</code>, is every extension.</p>"},{"location":"cli/#-exclude-name","title":"<code>--exclude NAME</code>","text":"<p>Drop the named extension from the selected set. The flag is repeatable. <code>--exclude</code> applies after <code>--use</code>, so you can start from all extensions and remove a few.</p> <p>Selection order does not matter. Stages always run in their defined priority order, matching the in-process behavior. An unknown name passed to either flag is a usage error (see exit codes).</p>"},{"location":"cli/#-warn-post-only","title":"<code>--warn</code> (<code>post</code> only)","text":"<p>Writes advisory diagnostics to stderr for markers the post stage sees but cannot fully apply. It changes no output and does not change the exit code.</p> <p>It reports the three conditions a post-only filter can detect:</p> <ul> <li>A malformed <code>mw-fence</code> JSON payload, which is skipped rather than executed.</li> <li>A marker whose <code>version</code> this tool does not support.</li> <li>A marker with no adjacent code block to style.</li> </ul> <p>Without <code>--warn</code>, each of these is a silent no-op. A renderer that strips the <code>mw-fence</code> comment outright is undetectable here, since the marker is simply gone; that case is covered by the renderer requirements, not by runtime detection.</p>"},{"location":"cli/#-version","title":"<code>--version</code>","text":"<p>Prints the installed package version and exits.</p>"},{"location":"cli/#exit-codes","title":"Exit Codes","text":"<ul> <li><code>0</code> on success.</li> <li><code>2</code> on a usage error: an unknown subcommand, or an unknown name passed to <code>--use</code> or <code>--exclude</code>.   The offending name is reported to stderr.</li> <li>A nonzero code (<code>1</code>) if an I/O error propagates, since the tool fails loud rather than swallowing it.</li> </ul>"},{"location":"cli/#the-canonical-pipeline","title":"The Canonical Pipeline","text":"<p>Bracket any renderer with the two stages:</p> <pre><code>mw pre &lt; in.md | some-renderer | mw post &gt; out.html\n</code></pre> <p>The pre stage prepares the source, your renderer turns Markdown into HTML, and the post stage applies the HTML-level styling and script injection. To select a subset of features, pass the same <code>--use</code> or <code>--exclude</code> flags to both stages:</p> <pre><code>mw pre --use youtube --use highlight &lt; in.md | some-renderer | mw post --use highlight &gt; out.html\n</code></pre> <p>For the full integration model, including when to run only the post stage, see the Pipeline Guide.</p>"},{"location":"demo/","title":"Demo","text":"<p>A demonstration of all markwright extensions on a single page.</p>"},{"location":"demo/#highlight","title":"Highlight","text":"<p>Inline highlight: This has a highlighted word in it.</p> <p>In inline code: Check <code>config.timeout</code> for the setting.</p> <p>In a code block:</p> <pre><code>def process(data):\n    result = transform(data)\n    return result\n</code></pre>"},{"location":"demo/#fence-labels","title":"Fence: Labels","text":"app.py <pre><code>from flask import Flask\n\napp = Flask(__name__)\n</code></pre>"},{"location":"demo/#fence-secondary-labels","title":"Fence: Secondary Labels","text":"<pre><code>OutputServer started on port 8080\n</code></pre>"},{"location":"demo/#fence-environments","title":"Fence: Environments","text":"local-server.sh <pre><code>ssh user@localhost\n</code></pre> staging-server.sh <pre><code>ssh deploy@staging.example.com\n</code></pre> production-server.sh <pre><code>ssh deploy@prod.example.com\n</code></pre>"},{"location":"demo/#fence-line-numbers","title":"Fence: Line Numbers","text":"<pre><code><ol><li>from flask import Flask\n</li><li>\n</li><li>app = Flask(__name__)\n</li><li>\n</li><li>@app.route(\"/\")\n</li><li>def index():\n</li><li>    return \"Hello, World!\"\n</li></ol>\n</code></pre>"},{"location":"demo/#fence-command-prefix","title":"Fence: Command Prefix","text":"<pre><code><ol><li>sudo apt update\n</li><li>sudo apt install nginx\n</li><li>systemctl start nginx\n</li></ol>\n</code></pre>"},{"location":"demo/#fence-super-user-prefix","title":"Fence: Super User Prefix","text":"<pre><code><ol><li>systemctl restart nginx\n</li><li>journalctl -u nginx -f\n</li></ol>\n</code></pre>"},{"location":"demo/#fence-custom-prefix","title":"Fence: Custom Prefix","text":"<pre><code><ol><li>SELECT * FROM users;\n</li><li>SHOW DATABASES;\n</li><li>USE production;\n</li></ol>\n</code></pre>"},{"location":"demo/#fence-full-combination","title":"Fence: Full Combination","text":"deploy.sh <pre><code><ol><li>ssh root@192.168.1.1\n</li><li>apt update &amp;&amp; apt upgrade -y\n</li><li>systemctl restart nginx\n</li></ol>\n</code></pre>"},{"location":"demo/#youtube","title":"YouTube","text":"&lt;a href=\"https://www.youtube.com/watch?v=dQw4w9WgXcQ\" target=\"_blank\"&gt;View YouTube video&lt;/a&gt;"},{"location":"demo/#codepen","title":"CodePen","text":"<p> See the Pen vwPzeX by MattCowley (@MattCowley) on CodePen. </p>"},{"location":"demo/#twitter","title":"Twitter","text":"View tweet by @github"},{"location":"demo/#instagram","title":"Instagram","text":"View post"},{"location":"demo/#slideshow","title":"Slideshow","text":"\u2039 \u203a"},{"location":"demo/#image-compare","title":"Image Compare","text":""},{"location":"pipeline/","title":"Pipeline Guide","text":"<p>The <code>mw</code> CLI lets you use markwright syntax in a toolchain that is not built on Python-Markdown. It brackets your existing renderer with two filters: a pre stage that runs on Markdown source, and a post stage that runs on the rendered HTML. This page covers the model and shows worked Hugo and plain-Unix examples.</p> <p>For the per-flag reference, see the CLI Reference.</p>"},{"location":"pipeline/#the-pre-render-post-model","title":"The Pre / Render / Post Model","text":"<p>Every markwright feature decomposes into at most two pure string transforms. A source-stage transform takes Markdown text and returns Markdown with embedded HTML and marker comments. An HTML-stage transform takes rendered HTML and returns rendered HTML. Your renderer runs between them.</p> <pre><code>flowchart LR\n    A[Markdown source] --&gt; B[mw pre]\n    B --&gt; C[your renderer]\n    C --&gt; D[mw post]\n    D --&gt; E[final HTML]</code></pre> <p>The pre stage expands the embed directives and extracts fence directives into an <code>mw-fence</code> comment. Your renderer turns Markdown into HTML, passing the raw HTML and the comments through. The post stage applies highlight, fence labels and prefixes, and one-time embed scripts.</p>"},{"location":"pipeline/#when-to-run-only-post","title":"When to Run Only Post","text":"<p>You do not always need both stages.</p> <p>Run both stages when your source contains pre-stage syntax: embed directives like <code>[youtube ID]</code>, or fence directives like <code>[label deploy.sh]</code>. The pre stage has to expand those before your renderer sees them.</p> <p>Run only the post stage when your HTML already carries the embed markup and any highlight markers, and you want the post-stage styling and script injection. The post stage detects each embed's class signature on its own, so it injects scripts for hand-authored embeds with no pre-stage marker. Highlight is also complete in the post stage: after a render, both prose and in-code markers surface as escaped text, and a single post pass handles both.</p> <p>Run only the pre stage when you want prose highlights resolved to <code>&lt;mark&gt;</code> before your renderer runs, and nothing else.</p> <p>The two stages are safe to combine. Pre resolves prose highlights, post resolves whatever markers remain, and running post twice changes nothing.</p>"},{"location":"pipeline/#plain-unix-example","title":"Plain Unix Example","text":"<p>The shortest path is a single pipe:</p> <pre><code>mw pre &lt; in.md | cmark --unsafe | mw post &gt; out.html\n</code></pre> <p><code>cmark --unsafe</code> is CommonMark with raw-HTML passthrough enabled, which the expanded embeds require. To run a subset, pass matching flags to both stages:</p> <pre><code>mw pre --exclude twitter --exclude instagram &lt; in.md \\\n  | cmark --unsafe \\\n  | mw post --exclude twitter --exclude instagram &gt; out.html\n</code></pre> <p>If your source has no embed or fence directives and you only want highlight styling, the post stage alone is enough:</p> <pre><code>cmark --unsafe &lt; in.md | mw post --use highlight &gt; out.html\n</code></pre>"},{"location":"pipeline/#hugo","title":"Hugo","text":"<p>Hugo is a worked, tested instance of this model, with Goldmark as the renderer. See the Hugo integration guide for the exact <code>hugo.toml</code> settings and a build script that brackets a real site.</p>"},{"location":"renderer-requirements/","title":"Renderer Requirements","text":"<p>The <code>mw pre</code> and <code>mw post</code> stages coordinate through markers that survive a normal Markdown render, not through any knowledge of the renderer. For that round-trip to work, the renderer you place between the two stages has to meet three requirements. Each requirement maps to a feature, and a renderer that does not meet one disables that feature while leaving the rest working.</p>"},{"location":"renderer-requirements/#the-three-requirements","title":"The Three Requirements","text":""},{"location":"renderer-requirements/#1-raw-html-passthrough","title":"1. Raw HTML Passthrough","text":"<p>The pre stage expands embed directives into raw HTML and, with highlight active, wraps prose markers in <code>&lt;mark&gt;</code>. A renderer that strips raw HTML by default has to enable its passthrough option (<code>--unsafe</code> for cmark, <code>unsafe = true</code> for Hugo's Goldmark, and the equivalent elsewhere). Without passthrough, the expanded embeds and pre-stage highlights are dropped by the renderer before the post stage ever sees them.</p>"},{"location":"renderer-requirements/#2-comment-preservation","title":"2. Comment Preservation","text":"<p>The pre stage encodes fence directives as an <code>&lt;!-- mw-fence:{JSON} --&gt;</code> comment placed immediately before the fence. The post stage reads that comment to style the adjacent code block. A renderer that preserves HTML comments lets fence styling round-trip. A renderer that drops comments disables fence styling and breaks nothing else: the code block still renders, just without its label, environment tint, or line prefixes.</p>"},{"location":"renderer-requirements/#3-span-based-syntax-highlighting","title":"3. Span-Based Syntax Highlighting","text":"<p>In-code highlighting (<code>...</code> inside a fence) depends on the renderer wrapping syntax-highlighted tokens in tags, so the post stage can place <code>&lt;mark&gt;</code> without crossing a token boundary. Any Pygments- or Chroma-style highlighter qualifies. A renderer that emits plain, untagged code text still highlights, but the <code>&lt;mark&gt;</code> placement has fewer token boundaries to respect.</p>"},{"location":"renderer-requirements/#the-mw-fence-marker-contract","title":"The <code>mw-fence</code> Marker Contract","text":"<p>The fence directives travel from <code>pre</code> to <code>post</code> as a single HTML comment immediately before the fence:</p> <pre><code>&lt;!-- mw-fence:{\"version\": 1, \"label\": \"deploy.sh\"} --&gt;\n</code></pre> <p>The fence itself is left intact, so your renderer still syntax-highlights the real code. There is no placeholder and no rewritten language token. The post stage associates a marker with the code block that immediately follows it. This comment is the only cross-tool surface, so its shape is a versioned contract.</p>"},{"location":"renderer-requirements/#v1-payload","title":"v1 Payload","text":"<pre><code>{\n  \"version\": 1,\n  \"label\": \"deploy.sh\",\n  \"secondary_label\": \"optional second label\",\n  \"environment\": \"local\",\n  \"prefix_type\": \"command\",\n  \"prefix_value\": \"$\"\n}\n</code></pre> <p><code>version</code> is an integer and the only required field. Every other field is optional and present only when the author used that directive. <code>prefix_type</code> is one of <code>line_numbers</code>, <code>command</code>, <code>super_user</code>, or <code>custom_prefix</code>. <code>prefix_value</code> carries the rendered prefix (<code>$</code>, <code>#</code>, or custom text) and is absent for <code>line_numbers</code>. The post stage applies whatever fields are present and ignores any it does not recognize.</p>"},{"location":"renderer-requirements/#versioning-policy","title":"Versioning Policy","text":"<p>The policy is best-effort, backward compatible, and fail-soft.</p> <p>The schema grows additively: a new optional field does not bump <code>version</code>, and only a breaking change does. The post stage reads any marker whose <code>version</code> it knows (currently only <code>1</code>) and ignores unknown fields, so a newer pre stage that only added fields still works with an older post stage. A marker whose <code>version</code> is greater than the post stage knows is skipped as a no-op and reported under <code>--warn</code>. A malformed payload (invalid JSON, or a missing or wrongly typed required field) is skipped, never executed, and reported under <code>--warn</code>.</p>"},{"location":"renderer-requirements/#what-degrades-and-how","title":"What Degrades, and How","text":"<p>The post stage degrades gracefully. If a marker or signature is absent, the matching feature is a no-op rather than an error.</p> Unmet Requirement Feature That Stops Behavior Raw HTML passthrough Embeds, pre-stage prose highlight The renderer drops the expanded HTML before post runs Comment preservation Fence labels, environments, prefixes Code blocks render unstyled; the skip is silent Span-based highlighting In-code highlight Markers still wrap, with fewer token boundaries to respect <p>The comment-stripping case is special. Once a renderer drops the <code>mw-fence</code> comment, the post stage has no evidence the directive ever existed, so a stripped marker is silently absent rather than a detectable skip. That is why <code>--warn</code> cannot report it: a post-only filter sees nothing to warn about. Embed script injection is unaffected, since it keys on the embed class signature in the HTML, not on any comment.</p>"},{"location":"extensions/codepen/","title":"CodePen","text":"<p>Embeds CodePen pens with configurable theme, default tab, height, and behavior flags.</p>"},{"location":"extensions/codepen/#usage","title":"Usage","text":"<pre><code>import markdown\n\nmd = markdown.Markdown(extensions=[\"markwright.codepen\"])\nhtml = md.convert(\"[codepen MattCowley vwPzeX]\")\n</code></pre> <p>See Using with MkDocs to load it in a MkDocs site.</p>"},{"location":"extensions/codepen/#syntax","title":"Syntax","text":"<pre><code>[codepen USER HASH flags...]\n</code></pre> Parameter Default Description <code>USER</code> required CodePen username <code>HASH</code> required Pen hash/slug"},{"location":"extensions/codepen/#flags","title":"Flags","text":"<p>Flags can appear in any order after USER and HASH:</p> Flag Description <code>light</code> Light theme (default) <code>dark</code> Dark theme <code>html</code> Show HTML tab <code>css</code> Show CSS tab <code>js</code> Show JS tab <code>result</code> Show result tab (default; combines with other tabs) <code>lazy</code> Lazy load the pen (adds <code>data-preview=\"true\"</code>) <code>editable</code> Allow editing (adds <code>data-editable=\"true\"</code>) integer Custom height in pixels (default: 256) <p>Tab priority when multiple are specified: html &gt; css &gt; js.</p>"},{"location":"extensions/codepen/#examples","title":"Examples","text":""},{"location":"extensions/codepen/#basic-embed","title":"Basic Embed","text":"<pre><code>[codepen MattCowley vwPzeX]\n</code></pre> <p> See the Pen vwPzeX by MattCowley (@MattCowley) on CodePen. </p>"},{"location":"extensions/codepen/#dark-theme-with-css-tab","title":"Dark Theme with CSS Tab","text":"<pre><code>[codepen MattCowley vwPzeX dark css]\n</code></pre> <p> See the Pen vwPzeX by MattCowley (@MattCowley) on CodePen. </p>"},{"location":"extensions/codepen/#custom-height-lazy-editable","title":"Custom Height, Lazy, Editable","text":"<pre><code>[codepen MattCowley vwPzeX 400 lazy editable]\n</code></pre> <p> See the Pen vwPzeX by MattCowley (@MattCowley) on CodePen. </p>"},{"location":"extensions/codepen/#notes","title":"Notes","text":"<ul> <li>The embed must be on its own line</li> <li>The CodePen embed script is injected once, even with multiple embeds on a page</li> </ul>"},{"location":"extensions/fence/","title":"Fence","text":"<p>Adds labels, secondary labels, environment classes, and line prefixes to fenced code blocks. All fence features are coordinated through a single extension to avoid ordering issues.</p>"},{"location":"extensions/fence/#usage","title":"Usage","text":"<p>Load <code>markwright.fence</code> alongside <code>pymdownx.superfences</code> and <code>pymdownx.highlight</code>; the fence postprocessor rewrites the code-block HTML those produce.</p> <pre><code>import markdown\n\nmd = markdown.Markdown(\n    extensions=[\n        \"pymdownx.superfences\",\n        \"pymdownx.highlight\",\n        \"markwright.fence\",\n    ],\n    extension_configs={\n        \"markwright.fence\": {\n            \"allowed_environments\": [\"local\", \"second\", \"third\", \"fourth\", \"fifth\"],\n        },\n    },\n)\n</code></pre> Option Default Description <code>allowed_environments</code> <code>[]</code> (allow all) List of allowed environment names. If empty, all names are accepted. <p>See Using with MkDocs to load it in a MkDocs site.</p>"},{"location":"extensions/fence/#labels","title":"Labels","text":"<p>Add a label above a code block with <code>[label TEXT]</code> as the first line inside the fence:</p> <pre><code>```python\n[label app.py]\nfrom flask import Flask\n\napp = Flask(__name__)\n```\n</code></pre> app.py <pre><code>from flask import Flask\n\napp = Flask(__name__)\n</code></pre>"},{"location":"extensions/fence/#secondary-labels","title":"Secondary Labels","text":"<p>Add a secondary label inside the code block with <code>[secondary_label TEXT]</code>:</p> <pre><code>```\n[secondary_label Output]\nHello, world!\n```\n</code></pre> <pre><code>OutputHello, world!\n</code></pre>"},{"location":"extensions/fence/#both-labels-together","title":"Both Labels Together","text":"<pre><code>```python\n[label app.py]\n[secondary_label Output]\nHello from Flask!\n```\n</code></pre> app.py <pre><code>OutputHello from Flask!\n</code></pre>"},{"location":"extensions/fence/#environments","title":"Environments","text":"<p>Tint the whole code block with a color using <code>[environment NAME]</code> inside the fence. This helps distinguish code blocks for different server environments:</p> <pre><code>```\n[environment local]\nssh user@local-server\n```\n</code></pre> <pre><code>ssh user@local-server\n</code></pre> <pre><code>```\n[environment second]\nssh user@staging-server\n```\n</code></pre> <pre><code>ssh user@staging-server\n</code></pre> <pre><code>```\n[environment third]\nssh user@production-server\n```\n</code></pre> <pre><code>ssh user@production-server\n</code></pre>"},{"location":"extensions/fence/#line-prefixes","title":"Line Prefixes","text":"<p>Prefix flags go in the info string (after the backticks). They wrap each code line in <code>&lt;ol&gt;&lt;li&gt;</code> with a <code>data-prefix</code> attribute.</p>"},{"location":"extensions/fence/#line-numbers","title":"Line Numbers","text":"<pre><code>```line_numbers,python\nfrom flask import Flask\n\napp = Flask(__name__)\n\n@app.route(\"/\")\ndef hello():\n    return \"Hello!\"\n```\n</code></pre> <pre><code><ol><li>from flask import Flask\n</li><li>\n</li><li>app = Flask(__name__)\n</li><li>\n</li><li>@app.route(\"/\")\n</li><li>def hello():\n</li><li>    return \"Hello!\"\n</li></ol>\n</code></pre>"},{"location":"extensions/fence/#command-prefix","title":"Command Prefix (<code>$</code>)","text":"<p>Implicitly sets the language to bash:</p> <pre><code>```command\nsudo apt update\nsudo apt install nginx\n```\n</code></pre> <pre><code><ol><li>sudo apt update\n</li><li>sudo apt install nginx\n</li></ol>\n</code></pre>"},{"location":"extensions/fence/#super-user-prefix","title":"Super User Prefix (<code>#</code>)","text":"<p>Implicitly sets the language to bash:</p> <pre><code>```super_user\nsystemctl restart nginx\n```\n</code></pre> <pre><code><ol><li>systemctl restart nginx\n</li></ol>\n</code></pre>"},{"location":"extensions/fence/#custom-prefix","title":"Custom Prefix","text":"<p>Implicitly sets the language to bash. Use <code>\\s</code> for spaces:</p> <pre><code>```custom_prefix(mysql&gt;)\nSELECT * FROM users;\nSHOW TABLES;\n```\n</code></pre> <pre><code><ol><li>SELECT * FROM users;\n</li><li>SHOW TABLES;\n</li></ol>\n</code></pre>"},{"location":"extensions/fence/#full-combination","title":"Full Combination","text":"<p>All features can be combined:</p> <pre><code>```command\n[environment local]\n[label server.sh]\nssh root@192.168.1.1\napt update &amp;&amp; apt upgrade -y\n```\n</code></pre> server.sh <pre><code><ol><li>ssh root@192.168.1.1\n</li><li>apt update &amp;&amp; apt upgrade -y\n</li></ol>\n</code></pre>"},{"location":"extensions/highlight/","title":"Highlight","text":"<p>Converts <code>&lt;^&gt;text&lt;^&gt;</code> to <code>&lt;mark&gt;text&lt;/mark&gt;</code>. Works in regular text, inline code, and fenced code blocks, anywhere that <code>pymdownx.mark</code> (<code>==text==</code>) cannot reach.</p>"},{"location":"extensions/highlight/#usage","title":"Usage","text":"<pre><code>import markdown\n\nmd = markdown.Markdown(extensions=[\"markwright.highlight\"])\nhtml = md.convert(\"This has a &lt;^&gt;highlighted word&lt;^&gt; in it.\")\n</code></pre> <p>See Using with MkDocs to load it in a MkDocs site.</p>"},{"location":"extensions/highlight/#syntax","title":"Syntax","text":"<p>Wrap text in <code>&lt;^&gt;</code> delimiters:</p> <pre><code>This has a &lt;^&gt;highlighted word&lt;^&gt; in it.\n</code></pre> <p>Result: This has a highlighted word in it.</p>"},{"location":"extensions/highlight/#in-inline-code","title":"In Inline Code","text":"<pre><code>Check the `config.&lt;^&gt;timeout&lt;^&gt;` value.\n</code></pre> <p>Result: Check the <code>config.timeout</code> value.</p>"},{"location":"extensions/highlight/#in-fenced-code-blocks","title":"In Fenced Code Blocks","text":"<pre><code>```python\ndef greet(name):\n    print(f\"Hello, &lt;^&gt;{name}&lt;^&gt;!\")\n```\n</code></pre> <p>Result:</p> <pre><code>def greet(name):\n    print(f\"Hello, {name}!\")\n</code></pre>"},{"location":"extensions/highlight/#multiple-highlights","title":"Multiple Highlights","text":"<p>Multiple highlights work on the same line:</p> <pre><code>Both &lt;^&gt;first&lt;^&gt; and &lt;^&gt;second&lt;^&gt; are highlighted.\n</code></pre> <p>Result: Both first and second are highlighted.</p>"},{"location":"extensions/highlight/#escaping","title":"Escaping","text":"<p>Prefix a marker with a backslash (<code>&lt;^&gt;</code>) to show it literally instead of applying a highlight. This works consistently in regular text, inline code, and fenced code blocks, and produces the same literal marker whether you render in-process or through the <code>mw pre | post</code> pipeline. This is how the code samples above print the raw <code>&lt;^&gt;</code> syntax.</p>"},{"location":"extensions/image-compare/","title":"Image Compare","text":"<p>Creates side-by-side image comparisons with an interactive slider control.</p>"},{"location":"extensions/image-compare/#usage","title":"Usage","text":"<pre><code>import markdown\n\nmd = markdown.Markdown(extensions=[\"markwright.image_compare\"])\nhtml = md.convert(\"[compare https://example.com/before.jpg https://example.com/after.jpg]\")\n</code></pre> <p>See Using with MkDocs to load it in a MkDocs site.</p>"},{"location":"extensions/image-compare/#syntax","title":"Syntax","text":"<pre><code>[compare URL1 URL2 HEIGHT WIDTH]\n</code></pre> Parameter Default Description <code>URL1</code> required Left image URL <code>URL2</code> required Right image URL <code>HEIGHT</code> <code>270</code> Widget height in pixels <code>WIDTH</code> <code>480</code> Widget width in pixels"},{"location":"extensions/image-compare/#examples","title":"Examples","text":""},{"location":"extensions/image-compare/#basic-comparison","title":"Basic Comparison","text":"<pre><code>[compare https://picsum.photos/id/10/480/270 https://picsum.photos/id/20/480/270]\n</code></pre>"},{"location":"extensions/image-compare/#custom-dimensions","title":"Custom Dimensions","text":"<pre><code>[compare https://picsum.photos/id/10/600/400 https://picsum.photos/id/20/600/400 400 600]\n</code></pre>"},{"location":"extensions/image-compare/#notes","title":"Notes","text":"<ul> <li>The embed must be on its own line</li> <li>Exactly 2 image URLs are required (no more, no fewer)</li> <li>The slider starts at 50% and uses a CSS custom property (<code>--value</code>) for positioning</li> <li>No script injection required; uses an inline <code>oninput</code> handler</li> </ul>"},{"location":"extensions/instagram/","title":"Instagram","text":"<p>Embeds Instagram posts with optional caption display, alignment, and width.</p>"},{"location":"extensions/instagram/#usage","title":"Usage","text":"<pre><code>import markdown\n\nmd = markdown.Markdown(extensions=[\"markwright.instagram\"])\nhtml = md.convert(\"[instagram https://www.instagram.com/p/CkQuv3_LRgS]\")\n</code></pre> <p>See Using with MkDocs to load it in a MkDocs site.</p>"},{"location":"extensions/instagram/#syntax","title":"Syntax","text":"<pre><code>[instagram URL flags...]\n</code></pre> Parameter Default Description <code>URL</code> required Post reference: a full URL, a scheme-less URL, or a bare shortcode"},{"location":"extensions/instagram/#accepted-url-forms","title":"Accepted URL forms","text":"<p>All of these resolve to the same post, and the embed permalink is canonicalized to <code>https://www.instagram.com/p/&lt;shortcode&gt;</code>:</p> <pre><code>[instagram https://www.instagram.com/p/CkQuv3_LRgS]\n[instagram instagram.com/p/CkQuv3_LRgS]\n[instagram CkQuv3_LRgS]\n</code></pre>"},{"location":"extensions/instagram/#flags","title":"Flags","text":"Flag Description <code>caption</code> Show the post caption <code>left</code> Left-align the post <code>center</code> Center-align (default) <code>right</code> Right-align the post integer Width in pixels (clamped 326-550, default: auto)"},{"location":"extensions/instagram/#examples","title":"Examples","text":""},{"location":"extensions/instagram/#basic-post","title":"Basic Post","text":"<pre><code>[instagram https://www.instagram.com/p/CkQuv3_LRgS]\n</code></pre> View post"},{"location":"extensions/instagram/#with-caption","title":"With Caption","text":"<pre><code>[instagram https://www.instagram.com/p/CkQuv3_LRgS caption]\n</code></pre> View post"},{"location":"extensions/instagram/#left-aligned-with-custom-width","title":"Left-Aligned with Custom Width","text":"<pre><code>[instagram https://www.instagram.com/p/CkQuv3_LRgS left 400]\n</code></pre> View post"},{"location":"extensions/instagram/#notes","title":"Notes","text":"<ul> <li>The embed must be on its own line</li> <li>Width is clamped to 326-550px (Instagram's supported range)</li> <li>The Instagram embed script is injected once per page</li> </ul>"},{"location":"extensions/slideshow/","title":"Slideshow","text":"<p>Creates image slideshows with left/right navigation arrows and scroll-snap behavior.</p>"},{"location":"extensions/slideshow/#usage","title":"Usage","text":"<pre><code>import markdown\n\nmd = markdown.Markdown(extensions=[\"markwright.slideshow\"])\nhtml = md.convert(\"[slideshow https://example.com/a.jpg https://example.com/b.jpg]\")\n</code></pre> <p>See Using with MkDocs to load it in a MkDocs site.</p>"},{"location":"extensions/slideshow/#syntax","title":"Syntax","text":"<pre><code>[slideshow URL1 URL2 ...URLs HEIGHT WIDTH]\n</code></pre> Parameter Default Description URLs required (1+) Image URLs (one or more) <code>HEIGHT</code> <code>270</code> Slideshow height in pixels <code>WIDTH</code> <code>480</code> Slideshow width in pixels <p>Height and width are optional trailing integers. They are distinguished from URLs by being purely numeric.</p>"},{"location":"extensions/slideshow/#examples","title":"Examples","text":""},{"location":"extensions/slideshow/#basic-slideshow","title":"Basic Slideshow","text":"<pre><code>[slideshow https://picsum.photos/id/10/480/270 https://picsum.photos/id/20/480/270 https://picsum.photos/id/30/480/270]\n</code></pre> \u2039 \u203a"},{"location":"extensions/slideshow/#custom-dimensions","title":"Custom Dimensions","text":"<pre><code>[slideshow https://picsum.photos/id/10/600/400 https://picsum.photos/id/20/600/400 400 600]\n</code></pre> \u2039 \u203a"},{"location":"extensions/slideshow/#notes","title":"Notes","text":"<ul> <li>The embed must be on its own line</li> <li>At least one image URL is required (a single-image slideshow is valid, matching upstream <code>do-markdownit</code>)</li> <li>Navigation arrows appear on left and right sides</li> <li>Uses CSS scroll-snap for smooth slide transitions</li> <li>No script injection required</li> </ul>"},{"location":"extensions/twitter/","title":"Twitter","text":"<p>Embeds tweets with configurable theme, alignment, and width. Accepts <code>twitter.com</code> and <code>x.com</code> references in several forms (full URL, scheme-less, or a bare <code>user/status/id</code>), canonicalizing the output to <code>twitter.com</code>.</p>"},{"location":"extensions/twitter/#usage","title":"Usage","text":"<pre><code>import markdown\n\nmd = markdown.Markdown(extensions=[\"markwright.twitter\"])\nhtml = md.convert(\"[twitter https://twitter.com/User/status/123]\")\n</code></pre> <p>See Using with MkDocs to load it in a MkDocs site.</p>"},{"location":"extensions/twitter/#syntax","title":"Syntax","text":"<pre><code>[twitter URL flags...]\n</code></pre> Parameter Default Description <code>URL</code> required Tweet reference: a full URL, a scheme-less URL, or a bare <code>user/status/id</code> (<code>twitter.com</code> or <code>x.com</code>)"},{"location":"extensions/twitter/#accepted-url-forms","title":"Accepted URL forms","text":"<p>All of these resolve to the same tweet:</p> <pre><code>[twitter https://twitter.com/User/status/123]\n[twitter https://x.com/User/status/123]\n[twitter twitter.com/User/status/123]\n[twitter User/status/123]\n</code></pre>"},{"location":"extensions/twitter/#flags","title":"Flags","text":"Flag Description <code>light</code> Light theme (default) <code>dark</code> Dark theme <code>left</code> Left-align the tweet <code>center</code> Center-align (default) <code>right</code> Right-align the tweet integer Width in pixels (clamped 250-550, default: 550)"},{"location":"extensions/twitter/#examples","title":"Examples","text":""},{"location":"extensions/twitter/#basic-tweet","title":"Basic Tweet","text":"<pre><code>[twitter https://twitter.com/github/status/1234567890]\n</code></pre> View tweet by @github"},{"location":"extensions/twitter/#dark-theme-left-aligned","title":"Dark Theme, Left-Aligned","text":"<pre><code>[twitter https://twitter.com/github/status/1234567890 dark left]\n</code></pre> View tweet by @github"},{"location":"extensions/twitter/#custom-width","title":"Custom Width","text":"<pre><code>[twitter https://twitter.com/github/status/1234567890 400]\n</code></pre> View tweet by @github"},{"location":"extensions/twitter/#notes","title":"Notes","text":"<ul> <li>The embed must be on its own line</li> <li><code>x.com</code> URLs are automatically converted to <code>twitter.com</code></li> <li>Width is clamped to 250-550px (Twitter's supported range)</li> <li>The Twitter widgets script is injected once per page</li> </ul>"},{"location":"extensions/youtube/","title":"YouTube","text":"<p>Embeds YouTube videos as responsive iframes with automatic aspect ratio calculation.</p>"},{"location":"extensions/youtube/#usage","title":"Usage","text":"<pre><code>import markdown\n\nmd = markdown.Markdown(extensions=[\"markwright.youtube\"])\nhtml = md.convert(\"[youtube dQw4w9WgXcQ]\")\n</code></pre> <p>See Using with MkDocs to load it in a MkDocs site.</p>"},{"location":"extensions/youtube/#syntax","title":"Syntax","text":"<pre><code>[youtube VIDEO_ID]\n[youtube VIDEO_ID HEIGHT]\n[youtube VIDEO_ID HEIGHT WIDTH]\n</code></pre> Parameter Default Description <code>VIDEO_ID</code> required The YouTube video ID <code>HEIGHT</code> <code>270</code> Height in pixels <code>WIDTH</code> <code>480</code> Width in pixels <p>The aspect ratio is calculated automatically from the dimensions.</p>"},{"location":"extensions/youtube/#examples","title":"Examples","text":""},{"location":"extensions/youtube/#default-dimensions-480x270","title":"Default Dimensions (480x270)","text":"<pre><code>[youtube dQw4w9WgXcQ]\n</code></pre>      &lt;a href=\"https://www.youtube.com/watch?v=dQw4w9WgXcQ\" target=\"_blank\"&gt;View YouTube video&lt;/a&gt;"},{"location":"extensions/youtube/#custom-dimensions","title":"Custom Dimensions","text":"<pre><code>[youtube dQw4w9WgXcQ 360 640]\n</code></pre>      &lt;a href=\"https://www.youtube.com/watch?v=dQw4w9WgXcQ\" target=\"_blank\"&gt;View YouTube video&lt;/a&gt;"},{"location":"extensions/youtube/#notes","title":"Notes","text":"<ul> <li>The embed must be on its own line (not inline with other text)</li> <li>The video ID is URL-encoded for safety</li> <li>A fallback link is included inside the iframe for accessibility</li> </ul>"},{"location":"integrations/hugo/","title":"Hugo","text":"<p>Hugo renders Markdown with Goldmark, not Python-Markdown, so the markwright extensions cannot run inside it. Instead, the <code>mw</code> CLI brackets Hugo: the pre stage runs on your content before Hugo builds, and the post stage runs on Hugo's output HTML. This is the same pre/render/post model described in the Pipeline Guide, with Hugo as the renderer in the middle.</p> <p>This page mirrors the project's own end-to-end integration test, so the configuration and commands below are exactly what is exercised in CI.</p>"},{"location":"integrations/hugo/#how-it-fits-together","title":"How It Fits Together","text":"<pre><code>flowchart LR\n    A[content/*.md] --&gt; B[mw pre]\n    B --&gt; C[hugo build]\n    C --&gt; D[mw post]\n    D --&gt; E[public/**/index.html]</code></pre> <p>The pre stage expands the embed directives and extracts the fence directives into <code>mw-fence</code> comments. Hugo's Goldmark renderer turns the Markdown into HTML, passing the raw HTML and the comments through. The post stage applies the fence labels and prefixes, resolves the in-code highlights, and injects the embed scripts.</p>"},{"location":"integrations/hugo/#prerequisites","title":"Prerequisites","text":"<ul> <li><code>markwright</code> installed, so the <code>mw</code> command is on your path. See the CLI Reference.</li> <li>A Hugo site.</li> </ul>"},{"location":"integrations/hugo/#hugo-configuration","title":"Hugo Configuration","text":"<p>Two settings in <code>hugo.toml</code> are required. Without them Hugo silently strips what markwright produces, with no error.</p> <pre><code>[markup.goldmark.renderer]\n  unsafe = true\n\n[markup.highlight]\n  noClasses = false\n</code></pre> <p><code>unsafe = true</code> lets Goldmark pass raw HTML through, which the expanded embeds and the <code>mw-fence</code> comment depend on. This is Goldmark's only way to preserve them; the default drops every raw-HTML node and the marker comment along with it.</p> <p><code>noClasses = false</code> makes Chroma, Hugo's syntax highlighter, emit CSS classes rather than inline styles. The fence styling and the line prefixes hook onto those classes, so class-based highlighting is required.</p> <p>See Renderer Requirements for the full contract and what degrades if a requirement is unmet.</p>"},{"location":"integrations/hugo/#running-the-pipeline","title":"Running the Pipeline","text":"<p>For a single page, run the pre stage on the source, build, then run the post stage on the rendered page:</p> <pre><code>mw pre  &lt; content/posts/deploy.md &gt; content/posts/deploy.md.tmp &amp;&amp; mv content/posts/deploy.md.tmp content/posts/deploy.md\nhugo\nmw post &lt; public/posts/deploy/index.html &gt; public/posts/deploy/index.html.tmp &amp;&amp; mv public/posts/deploy/index.html.tmp public/posts/deploy/index.html\n</code></pre> <p>The pre stage rewrites the content in place, Hugo builds it, and the post stage rewrites the rendered HTML in place. Pass the same <code>--use</code> or <code>--exclude</code> flags to both stages if you want a subset of the extensions.</p>"},{"location":"integrations/hugo/#wrapping-it-in-a-build-script","title":"Wrapping It in a Build Script","text":"<p>A real site applies the pre stage to every content file before the build and the post stage to every rendered page after it:</p> <pre><code>#!/usr/bin/env bash\nset -euo pipefail\n\n# Pre-process every Markdown file in place.\nfind content -name '*.md' -print0 | while IFS= read -r -d '' file; do\n  mw pre &lt; \"$file\" &gt; \"$file.tmp\" &amp;&amp; mv \"$file.tmp\" \"$file\"\ndone\n\nhugo\n\n# Post-process every rendered page in place.\nfind public -name '*.html' -print0 | while IFS= read -r -d '' file; do\n  mw post &lt; \"$file\" &gt; \"$file.tmp\" &amp;&amp; mv \"$file.tmp\" \"$file\"\ndone\n</code></pre> <p>Run this against a copy of <code>content/</code>, or commit only the original source, since the pre stage rewrites the files. markwright special-cases nothing about Hugo: it is a pair of stdin-to-stdout filters, and Hugo is one renderer in the middle.</p>"},{"location":"integrations/hugo/#what-you-get","title":"What You Get","text":"<p>A source file looks like any other Markdown, with markwright syntax mixed in:</p> <pre><code>[youtube dQw4w9WgXcQ]\n\nA highlighted word.\n\n```command\n[label deploy.sh]\n./deploy.sh --prod\n```\n</code></pre> <p>After the full pipeline:</p> <ul> <li><code>[youtube dQw4w9WgXcQ]</code> becomes a responsive iframe.</li> <li>The prose <code>highlighted</code> becomes <code>&lt;mark&gt;highlighted&lt;/mark&gt;</code>.</li> <li>The command fence renders with its <code>deploy.sh</code> label and a <code>$</code> prompt on each line, and the <code>command</code> info string is highlighted as Bash.</li> </ul> <p>The <code>mw-fence</code> comment that carried the fence directives is consumed by the post stage and does not appear in the final HTML.</p>"},{"location":"integrations/mkdocs/","title":"Using with MkDocs","text":"<p>markwright is a set of Python-Markdown extensions, so it works in any Python-Markdown consumer. This page covers the MkDocs case. Each extension page shows the plain Python-Markdown usage.</p> <p>To use the extensions in a MkDocs site, add them under <code>markdown_extensions</code> in <code>mkdocs.yml</code>. List only the ones you need.</p> <pre><code>markdown_extensions:\n  - pymdownx.superfences\n  - pymdownx.highlight:\n      pygments_lang_class: true\n  - markwright.highlight\n  - markwright.fence:\n      allowed_environments:\n        - local\n        - second\n        - third\n        - fourth\n        - fifth\n  - markwright.youtube\n  - markwright.codepen\n  - markwright.twitter\n  - markwright.instagram\n  - markwright.slideshow\n  - markwright.image_compare\n</code></pre> <p>The fence extension expects <code>pymdownx.superfences</code> and <code>pymdownx.highlight</code> to be loaded too. Without them the fence preprocessor still extracts directives, but the code-block HTML may not match what the postprocessor rewrites.</p> <p>MkDocs 2.0 Compatibility</p> <p>MkDocs 2.0 removes the plugin system and rewrites the theming architecture, breaking all existing plugins and themes including Material for MkDocs. There is no migration path. This project pins MkDocs 1.x (<code>mkdocs&gt;=1.6,&lt;2</code>).</p>"}]}